(function(window, _, angular, undefined) {
'use strict';

var module = angular.module('page.home', [
  // module dependencies go here
  'component1',
]);

module.controller('HomeCtrl',
function($scope) {
  // page logic goes here
});

})(window, window._, window.angular);
